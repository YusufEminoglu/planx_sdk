version: 2
updates:
  # Keep GitHub Actions versions current so the workflows never silently
  # drift onto a deprecated runner (e.g. the Node 20 deprecation).
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "ci"
